# Schema

(To be completed after database implementation)

Questions to answer:

- Table by table: what columns and types does each one have?
- Which relationships are one-to-many?
- Which relationships are many-to-many?
- Which constraints are enforced by database?
- Which constraints are enforced by application code?
- What was deliberately denormalised?
- What would break first at 100x data?
